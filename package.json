{
  "name": "markdownpreview-editor-tests",
  "private": true,
  "description": "Unit tests for the in-place markdown editing core (MarkdownPreview/Resources/editor-core.js)",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
